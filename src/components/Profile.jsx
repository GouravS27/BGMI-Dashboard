import OverviewProfile from "./OverviewProfile"
import StatGrid from "./StatGrid"

const Profile = () => {
  return (
    <div>
      <OverviewProfile/>
      <StatGrid/>
    </div>
  )
}

export default Profile